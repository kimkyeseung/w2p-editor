# 프로젝트: W2P 스타일 캔버스 그래픽 에디터 (미니)

## 배경 / 목적

위시켓 "React/TypeScript 기반 W2P 그래픽 에디터 프론트엔드 개발" 공고에 지원하기 위한 포트폴리오용 개인 프로젝트입니다. 공고의 필수/우대 기술을 최대한 실제로 다뤄본 증거로 남기는 것이 목표입니다. 아래 공고 요구사항에 최대한 맞춰서 설계·구현해주세요.

- 필수: TypeScript, React, React 기반 컴포넌트 설계
- 필수: Zustand/Redux 등 상태관리 라이브러리로 복잡한 UI 상태 설계
- 필수: REST/GraphQL API 연동 및 반응형/모바일웹 개발 경험
- 우대: **Canvas 2D, SVG, 좌표 변환, Fabric.js**
- 우대: Figma/Canva/Photoshop류 그래픽 에디터 서비스 개발 경험
- 우대: 재사용 가능한 공통 컴포넌트 및 디자인 시스템 설계/운영 경험
- 우대: 대규모 SPA 성능 최적화 경험
- 우대: AI 기반 개발 도구 활용 경험

시간이 촉박한 상황(지원 마감 임박)이므로, 아래 MVP 범위를 먼저 완성하고 남는 시간에 스트레치 목표를 진행하는 순서로 작업해주세요. 완성도보다 "실제로 동작하는 상태"를 우선하세요.

## 컨셉

인쇄물(명함, 포스터 등)에 텍스트/이미지를 배치해서 디자인하는 웹 기반 W2P(Web-to-Print) 그래픽 에디터의 축소판. 사용자가 캔버스 위에서 레이어를 추가·편집하고, 저장하고, 이미지로 내보낼 수 있습니다.

## 기술 스택 (고정)

- React 18 + TypeScript
- Vite
- Zustand — 에디터 상태(레이어 목록, 선택된 객체, undo/redo 히스토리) 관리
- Fabric.js — 캔버스 렌더링, 객체 선택/이동/리사이즈/회전(transform), 좌표 변환
- 반응형 CSS (모바일 터치 대응 포함)

## MVP 기능 (우선순위 순, 이것부터 완성)

1. **캔버스 워크스페이스**
   - 인쇄 템플릿 사이즈 프리셋(예: 명함 90x50mm, 포스터 A4) 선택
   - 재단선(bleed)과 안전 영역(safe area) 가이드라인 표시
2. **텍스트 레이어**
   - 캔버스에 텍스트 객체 추가
   - 폰트 패밀리, 크기, 색상, 정렬(좌/중/우) 편집 가능한 속성 패널
3. **이미지 레이어**
   - 로컬 이미지 업로드 → 캔버스에 배치
   - Fabric.js의 transform 핸들로 드래그 이동 / 리사이즈 / 회전
4. **레이어 패널**
   - 캔버스 내 모든 객체를 목록으로 표시
   - 순서 변경(맨 앞으로/맨 뒤로), 잠금, 삭제, 복제
5. **선택 객체 속성 패널**
   - 선택된 객체의 x/y/width/height/rotation을 숫자 입력으로 직접 수정
   - 정렬 버튼(캔버스 기준 가운데 정렬 등)
6. **Undo / Redo**
   - Zustand로 히스토리 스택 관리, 키보드 단축키(Cmd/Ctrl+Z, Shift+Cmd/Ctrl+Z) 지원
7. **저장 / 불러오기**
   - 캔버스 상태를 JSON으로 직렬화(Fabric의 `toJSON`/`loadFromJSON` 활용)하여 로컬(localStorage 또는 파일 다운로드)에 저장·복원
8. **PNG 내보내기**
   - `canvas.toDataURL()` 기반으로 현재 디자인을 PNG로 다운로드
9. **반응형 / 모바일 대응**
   - 터치 드래그, 핀치 줌 등 모바일 웹에서도 기본적인 편집이 가능하도록 처리

## 스트레치 목표 (MVP 끝나고 시간 남으면)

1. **상품 목업 미리보기** — 티셔츠/머그컵 등 목업 이미지 위에 디자인을 오버레이해서 미리보기 (W2P 서비스의 핵심 가치를 가장 잘 보여주는 기능이라 우선순위 높음)
2. 다중 선택 및 그룹화
3. Figma 스타일 스마트 가이드(요소 간 정렬 시 스냅 라인)
4. 더미 REST API(json-server 등)를 붙여 "디자인 프로젝트 목록 저장/불러오기" API 연동 흐름 시연 (공고의 REST/GraphQL 연동 요구사항 어필용)
5. 재사용 가능한 컴포넌트 라이브러리화 (버튼, 패널, 입력 필드 등 디자인 시스템 형태로 분리)

## 구조 제안

```
src/
  components/
    Canvas/           - Fabric.js 캔버스 래퍼 컴포넌트
    LayerPanel/        - 레이어 목록 패널
    PropertiesPanel/   - 선택 객체 속성 편집 패널
    Toolbar/            - 텍스트 추가, 이미지 업로드, 내보내기 등 툴바
  store/
    editorStore.ts      - Zustand 스토어 (레이어, 선택, 히스토리)
  types/
    editor.ts           - 레이어/객체 타입 정의
  utils/
    canvasSerialization.ts - 저장/불러오기 로직
```

## 완료 기준 (Definition of Done)

- `npm run dev`로 로컬에서 정상 구동
- `npm run build`가 타입 에러 없이 성공
- 위 MVP 1~9번이 실제로 동작 (버튼 눌러서 확인 가능한 수준)
- README에 실행 방법과, 이 프로젝트가 공고의 어떤 요구사항(Fabric.js, Zustand, 반응형 등)에 대응하는지 짧게 정리
- 가능하면 Vercel/Netlify 등에 배포해서 라이브 링크로도 보여줄 수 있게 하기 (지원서에 링크 첨부 가능)

## 톤

이건 실제로 지원서에 "직접 만든 개인 프로젝트"로 첨부할 물건입니다. 과장되게 만들지 말고, 실제로 손으로 다뤄서 인터뷰에서 코드 설명이 가능한 수준으로 완성해주세요.
